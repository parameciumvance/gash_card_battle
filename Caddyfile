# 現在:純 IP 直連,監聽所有介面的 80 埠(不綁定特定網域,不需要填 VPS 的實際 IP 位址)。
# 之後補網域:把下面的 `:80` 直接換成你的網域(如 example.com),不用刪除或註解掉這行,
# 改完重啟 caddy 服務即可——Caddy 會自動偵測到這是合法網域格式,走 ACME 流程簽發並續期
# HTTPS 憑證,同時保留 80 埠處理 ACME 挑戰與導向 443,不需要另外裝 certbot 或手動管理憑證。
:80 {
	reverse_proxy app:8000
}
