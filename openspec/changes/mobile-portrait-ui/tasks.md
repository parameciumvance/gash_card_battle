# Tasks: mobile-portrait-ui

## 1. 尺寸變數化(桌面零視覺變化)

- [x] 1.1 style.css 卡片/框格/間距尺寸(104/150/75 等)抽為 `:root` CSS 變數;所有引用點改用變數
- [x] 1.2 桌面 E2E 回歸(既有 e2e 腳本全綠、視覺不變)

## 2. 行動按鈕統一入放大檢視(桌面互動變更)

- [x] 2.1 放大檢視改「卡片實例面板」:開啟時帶實例上下文(slot/page/partner/純展示),按鈕生成邏輯自 `slotEl`/`cardEl` 遷移(含 canActNow/合法性過濾與禁用原因)
- [x] 2.2 卡面 `.btns` 移除;卡片 click 一律開檢視;按下行動即送指令並關閉
- [x] 2.3 狀態更新時刷新開啟中的檢視(實例消失自動關閉);純展示場合無按鈕區
- [x] 2.4 E2E:出術/放置/攻擊/效果經檢視可完成;卡面無按鈕;對手卡/非行動時檢視無按鈕;檢視開啟中狀態變更不送過時指令

## 3. 手機直向盤面

- [x] 3.1 `isNarrow()`(matchMedia ≤700px)+ 變更監聽觸發 re-render;手機尺寸組變數
- [x] 3.2 窄螢幕堆疊佈局:對手魔本→搭檔→魔物→中線→我方魔物→搭檔→魔本;MP 與魔本同列;盤面縱向捲動 + action-bar sticky
- [x] 3.3 log 抽屜(頁籤顯示最新一條、展開/收合);topbar 緊湊化
- [x] 3.4 動畫(翻頁/FLIP/魔力對決/硬幣)於窄佈局驗證,異常降級
- [x] 3.5 E2E(viewport 375×812):本機局完整打數回合;堆疊序正確;無橫向捲動;觸控目標 ≥44px

## 4. 構築器手機直向

- [x] 4.1 窄螢幕上下分割(魔本上/卡池下、各自捲動);篩選收合為一列;合法性提示恆可見
- [x] 4.2 E2E(375×812):點選放卡/移除/兩點互換、篩選、儲存與匯出匯入可完成;桌面構築器回歸不變

## 5. 收尾

- [x] 5.1 全套桌面 E2E + pytest 回歸;i18n 新字串(抽屜、檢視按鈕等)
- [x] 5.2 手機實機(或 DevTools 模擬)冒煙:經通道連結加入線上局並打完開局流程
