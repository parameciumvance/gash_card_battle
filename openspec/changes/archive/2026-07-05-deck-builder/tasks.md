## 1. 後端:開局攜帶牌組

- [x] 1.1 CreateRoom/join 請求模型加可選 `deck`(preset 或 pages;本機房 `decks:[p0,p1]`),以 `validate_deck` 驗證,違規回 422 + DeckError 原因碼
- [x] 1.2 Room 暫存雙方牌組;`_start_game` 改為 `new_game(decks=(deck0, deck1))`;缺省 level1 行為不變
- [x] 1.3 後端測試:合法自訂牌組開局後雙方魔本互異(各自視角斷言)、非法牌組 422(逐條規則各一例)、缺省回歸、join 帶非法牌組不佔玩家位

## 2. 前端:牌組資料層

- [x] 2.1 localStorage 牌組庫(`gash-decks`):建立/改名/刪除/複製(空白、level1、既有牌組起手)、合法性標記與草稿自動保存
- [x] 2.2 前端七條構築規則驗證(JS 版,中文訊息 key 進 i18n),與後端共用同一組非法樣例驗證一致性
- [x] 2.3 文字碼匯出/匯入:`gash1:` 格式、未知卡號與格式錯誤拒絕、round-trip 一致

## 3. 前端:構築器視圖

- [x] 3.1 構築器版面(`/?builder=1`):卡池瀏覽(類型/對應魔物篩選)+ 16 對頁網格(P1/P32 獨立標示)
- [x] 3.2 編輯互動:點卡池放卡(選中頁位或下一空頁)、點頁位選中/移除、HTML5 拖拉互換(退化:點選兩頁位互換)
- [x] 3.3 即時驗證列:違規逐條顯示、修正即消失;儲存/另存/匯出/匯入操作列
- [x] 3.4 i18n:構築器與牌組管理全部字串
- [x] 3.5 卡池產品篩選:抽取補存 Sets 欄(重產 cards.json),構築器加產品下拉(可與類型/魔物複合)

## 4. 前端:開局選牌組

- [x] 4.1 首頁第 4 入口「牌組構築」;本機/建房/加入面板加牌組選單(level1 + 合法牌組;不合法禁用附提示)
- [x] 4.2 牌組隨 create/join 請求送出;本機房雙方各選

## 5. 驗證與收尾

- [x] 5.1 headless 瀏覽器 E2E:構築(放卡→違規提示→修正→儲存)→ 匯出→匯入還原 → 帶自訂牌組開本機局 → 線上房雙方各帶牌組對戰且互看不到對方翻開頁
- [x] 5.2 全量測試綠燈;README 加構築器與牌組碼說明
